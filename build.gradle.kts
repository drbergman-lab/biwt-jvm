// Root build file — shared config for all subprojects.
// Per-subproject configuration lives in core/build.gradle.kts and qupath-extension/build.gradle.kts.

allprojects {
    group = "io.github.drbergmanlab.biwt"
    version = "0.5.1"
}
